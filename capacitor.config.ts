import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ngxcovalent.app',
  appName: 'ngxcovalent',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
